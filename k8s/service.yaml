apiVersion: v1
kind: Service
metadata:
  name: iris-service
  labels:
    app: iris-service
spec:
  selector:
    app: iris-service
  ports:
    - name: http
      port: 80
      targetPort: http
  type: ClusterIP
