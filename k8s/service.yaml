apiVersion: v1
kind: Service
metadata:
  name: credit-scoring-service
  labels:
    app: credit-scoring-service
spec:
  selector:
    app: credit-scoring-service
  ports:
    - name: http
      port: 80
      targetPort: http
  type: ClusterIP
